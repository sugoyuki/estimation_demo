# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=calibration_system

# PostgreSQLユーザー名
# macOS (Homebrew): 通常はシステムユーザー名（例: yukisugo）
# Linux: 通常は postgres
DB_USER=postgres

# パスワード（ローカル開発では空欄でOKな場合が多い）
DB_PASSWORD=

# Server Configuration
PORT=3000
NODE_ENV=development
